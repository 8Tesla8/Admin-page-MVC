@model Uspa.Domain.LocalDb.Menus

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Menus</h4>
    <hr />
    <dl class="dl-horizontal">
        @*<dt>
        @Html.DisplayNameFor(model => model.previd)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.previd)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.parentidPrev)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.parentidPrev)
        </dd>*@

        <dt>
            @Html.DisplayNameFor(model => model.parentid)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.parentid)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.title)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.title)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.state)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.state)
        </dd>

        <dt>
            @*@Html.DisplayNameFor(model => model.Contents.title)*@ Content title
        </dt>

        <dd>
            @* Если поле пустое делаем просто бр чтобы верстка не ехала :\ *@
            @if (Model.Contents == null || Model.Contents.title == null)
            {
                <br />
            }
            else
            {
                @Html.DisplayFor(model => model.Contents.title)
            }
        </dd>

        <dt>
            @*@Html.DisplayNameFor(model => model.MenuTypes.title)*@ Menu type
        </dt>

        <dd>
            @if (Model.MenuTypes == null || Model.MenuTypes.title == null)
            {
                <br />
            }
            else
            {
                @Html.DisplayFor(model => model.MenuTypes.title)
            }
        </dd>

        <dt>
            @*@Html.DisplayNameFor(model => model.Sites.title)*@ Site title
        </dt>

        <dd>
            @if (Model.Sites == null || Model.Sites.title == null)
            {
                <br />
            }
            else
            {
                @Html.DisplayFor(model => model.Sites.title)
            }
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
